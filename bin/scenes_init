#! /usr/bin/env bash

scene_init() {
    jq ". += {\"events\":[], \"vsrc\":\"${1#**/}.mp4\"}" <<< '{}' > "$1/scene.json"
    echo -e "EXT_IN = .png\nFPS = 10" > "$1/config.mk"
}

export -f scene_init

find "$1" -mindepth 1 -type d -execdir bash -c 'scene_init "$0"' {} \;
